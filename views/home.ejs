<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zooming Image with Text</title>
    <style>
        body,
        html {
            height: 100%;
            margin: 0;
            overflow: hidden;
        }

        .container {
            position: relative;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .container::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.3);
            z-index: 1;
        }

        /* Add the image and apply animations */
        .zoom-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            animation: zoom 10s ease-in-out infinite alternate;
        }

        /* Define the zooming animation */
        @keyframes zoom {
            0% {
                transform: scale(1);
            }

            100% {
                transform: scale(1.2);
            }
        }

        .text {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 3rem;
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            text-transform: capitalize;
            z-index: 2;
            letter-spacing: 2.5px;
            padding-left: 0.5rem;
            padding-right: 0.5rem;
        }

        .button-link {
            display: inline-block;
            margin-top: 5rem;
            /* Add some space between text and button */
            padding: 10px 20px;
            background-color: transparent;
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-size: 18px;
            transition: background-color 0.3s ease;
            border: 1px solid white;
            cursor: pointer;
        }

        .button-link:hover {
            background-color: white;
            color: black;
        }
    </style>
</head>

<body>
    <div class="container">
        <img src="https://images.unsplash.com/photo-1631635589499-afd87d52bf64?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2671&q=80"
            alt="Zooming Image" class="zoom-image">

        <div class="text">
            <a href="/campgrounds" style="text-decoration: none; color: inherit;">enter the world of campgrounds</a>
            <button onclick="window.location.href='/campgrounds'" class="button-link">Explore Now</button>
            <!-- Button using button element -->
        </div>
    </div>
</body>

</html>